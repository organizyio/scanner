[package]
name = "scanner-format"
version = "0.2.1"
edition = "2021"
license = "MIT"
description = "Format detection helpers used by scanner-core."
repository = "https://github.com/organizyio/scanner"

[lib]
name = "format"

[dependencies]
file-format = "0.26"
thiserror = "2"
